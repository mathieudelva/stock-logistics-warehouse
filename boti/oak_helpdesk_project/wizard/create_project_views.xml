<odoo>
    <record id="create_project_view_form" model="ir.ui.view">
        <field name="name">Create a new Project from this ticket</field>
        <field name="model">helpdesk.create.project</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group col="2">
                        <group>
                            <field name="helpdesk_ticket_id" invisible="1" />
                            <field name="name" />
                            <field name="company_id" invisible="1" />
                            <field
                                name="project_tmpl_id"
                                widget="Many2one"
                                options="{'no_create': 1, 'no_edit': 1}"
                            />
                            <field name="note" invisible="1" />
                        </group>
                    </group>
                </sheet>
                <footer>
                    <button
                        string="Create &amp; View Project"
                        class="btn btn-primary"
                        name="action_generate_and_view_project"
                        type="object"
                    />
                    <button
                        string="Create Project"
                        class="btn btn-primary"
                        name="action_generate_project"
                        type="object"
                    />
                    <button
                        string="Discard"
                        class="btn btn-secondary"
                        special="cancel"
                    />
                </footer>
            </form>
        </field>
    </record>
</odoo>
