<?xml version="1.0" ?>
<odoo>
    <!--
    Only users with this group can view the fields with this security group.
    Add group to user_root to avoid errors when other modules install and
    add fields to views with this group defined as field attribute.
    -->
    <record model="res.groups" id="group_product_cost">
        <field name="name">Access to Product Cost Adjustments</field>
        <field name="category_id" ref="base.module_category_hidden" />
        <field name="users" eval="[(4, ref('base.user_root'))]" />
    </record>

    <record model="ir.rule" id="stock_cost_adjustment_rule">
        <field name="name">stock_cost_adjustment_rule multi-company</field>
        <field name="model_id" search="[('model','=','stock.cost.adjustment')]" model="ir.model"/>
        <field name="domain_force">['|',('company_id', 'in', company_ids),('company_id','=',False)]</field>
    </record>
    
</odoo>
