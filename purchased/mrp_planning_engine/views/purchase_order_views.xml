<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.form</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <field name="price_unit" position="after">
                <field name="mto_origin" />

            </field>
            <!-- <button name="button_confirm" position="after">
                <button name="intercompany_flow" string="Create Intercompany Flow" type="object" class="oe_highlight"
                    attrs="{'invisible': ['|', ('intercompany','=', False), ('sales_intercompany','!=', False)]}"/>
                <field name="intercompany" invisible="1"/>
                <field name="sales_intercompany" invisible="1"/>
            </button> -->
        </field>
    </record>
</odoo>
